---
---

<section>
  <div class="section-container">
    <div class="left">
      <p class="first"><i>#RJ23</i> 5-6 mai 2023</p>
      <!-- <a class="global-link" href="/2023"></a> -->
      <div class="mask"></div>
      <p class="second">multi-sites : <i>Genève, Oron, Tavannes</i></p>
    </div>
    <div class="right">
      <iframe
        width="100%"
        height="100%"
        src="https://www.youtube.com/embed/MsNQQ6CEBwE"
        title="YouTube video player"
        frameborder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowfullscreen></iframe>
    </div>
  </div>
</section>

<style lang="scss">
  @import "../../styles/variables";

  section {
    .section-container {
      p.date {
        font-style: italic;
        font-weight: bold;
        font-size: $sub-title;
      }

      p.location {
        font-size: $medium-title;
        padding: 20px 0 0 0;
      }
    }
  }
</style>

<style lang="scss">
  @import "../../styles/variables";
  @import "../../styles/button-bar";

  section {
    .section-container {
      height: 330px;
      padding: 0 0 0 $blank-space;

      display: flex;
      flex-direction: row;

      div.left {
        text-align: right;
        padding: 0 $sub-padding 0 0;
        position: relative;

        flex: 2;
        display: flex;
        justify-content: flex-end;
        align-items: center;
        flex-direction: column;

        font-weight: bold;

        background: #2c6425; /* Old browsers */
        background: linear-gradient(45deg, #8df553 0%, #2c6425 100%);

        &::before {
          content: "";
          flex: 1;
        }
        &::after {
          content: "";
          flex: 1;
        }

        p.first {
          color: #fff;

          text-shadow: 0 0 2px #b5ad00, 0 0 18px #eae9d3;

          font-size: $title-size;
          line-height: 0.9em;
          padding: 0 0 19px 0;
        }

        p.second {
          color: #fff;
          font-size: $medium-title;
          line-height: 0.9em;
        }

        a.global-link {
          position: absolute;
          top: 0;
          right: 0;
          bottom: 0;
          left: 0;
          transition: all 0.5s;
          z-index: 2;
        }

        .mask {
          position: absolute;
          top: 0;
          right: 0;
          bottom: 0;
          left: 0;
          transition: all 0.5s;
          z-index: 1;
        }

        &:hover {
          & .mask {
            background-color: rgba($color: rgb(255, 255, 255), $alpha: 0.2);
          }
        }
      }

      div.right {
        background-image: url("/home/vision/background.jpg");
        background-size: cover;
        background-position: center left;

        flex: 1;
      }
    }
    @media only screen and (max-width: $mobile-width) {
      .section-container {
        padding: 0 !important;

        div.poster,
        div.title {
          flex: 1 !important;
        }
      }
    }
  }
</style>
