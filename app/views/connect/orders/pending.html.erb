<% content_for :id, "orders-pending" %>

<%= content_for :header do %>
  <%= render "connect/page_header",
    link: connect_orders_path,
    title: "Commandes en cours" %>
<% end %>

<% if @orders.empty?%>

  <p>Aucune commande en cours.</p>

<% end %>

<% @orders.each do |order| %>

  <%= order.order_id %> /
  <%= l order.created_at, format: :pretty_date %> /
  <%= link_to "Continuer", edit_orders_event_path(order.order_id) %>
  <%= link_to "Annuler", order_path(order.order_id), method: :delete, data: { confirm: "Etes-vous sûr de vouloir annuler votre commande ?" } %>

<% end %>
