<% content_for :id, "orders-index" %>

<section class="content-wrap">

  <div id="connect-header">
    <%= link_to image_tag("connect/layout/back.svg", class: "back"), connect_root_path, remote: false %>
    <h1>Mes commandes</h1>
  </div>

  <% @orders.each do |order| %>

    <%= order.order_id %>

  <% end %>

</section>