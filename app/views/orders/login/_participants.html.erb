<div class="fields-for wrap">
  <%= product_f.fields_for :participants do |f| %>
    <div class="participants-fields wrap">

      <div class="participants-wrap">
        <%= f.label :gender %><div class="select"><%= f.select :gender, options_for_enum(f.object, :gender) %></div>

        <div>
          <%= f.label :firstname %>
          <%= f.field_with_errors :text, :firstname, autocomplete: "off" %>
        </div>

        <div>
          <%= f.label :lastname %>
          <%= f.field_with_errors :text, :lastname, autocomplete: "off" %>
        </div>

        <div>
          <%= f.label :email %>
          <%= f.field_with_errors :text, :email, autocomplete: "off" %>
        </div>

        <div class="age number-wo-arrows">
          <%= f.label :age %>
          <%= f.field_with_errors :number, :age, autocomplete: "off" %>
        </div>
      </div>

      <%= f.hidden_field :_destroy, identifier: "destroy" %>

      <button type="button" class="remove-fields" data-type="participants">Supprimer</button>
    </div>

    <button type="button" class="add-fields" data-type="participants" data-count="<%= participants.size %>">
      Ajouter un forfait
    </button>

  <% end %>
</div>
