<h1>Commandes</h1>

<%= form_tag nil, method: :get do %>

  <%= select_tag :key, options_for_select(@keys, params[:key]) %>

  <%= submit_tag "Sélectionner" %>

<% end %>

<%= @table.present(buttons: "buttons") %>
